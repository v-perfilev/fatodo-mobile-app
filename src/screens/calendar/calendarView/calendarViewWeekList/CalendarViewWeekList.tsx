import React, {memo} from 'react';
import {Box} from 'native-base';

type CalendarViewWeekListProps = {};

const CalendarViewWeekList = ({}: CalendarViewWeekListProps) => {
  return <Box />;
};

export default memo(CalendarViewWeekList);
